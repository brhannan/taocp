cmake_minimum_required(VERSION 3.14)
project(comparison_count)
include_directories("${PROJECT_SOURCE_DIR}/include")
file(GLOB sources "${PROJECT_SOURCE_DIR}/src/*.c")
add_executable(comparison_count ${sources})